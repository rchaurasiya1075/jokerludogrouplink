<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>ASCII Animation</title>
  <style>
    body {
      background-color: black;
      color: lime;
      font-family: monospace;
      white-space: pre;
      font-size: 16px;
      text-align: center;
      padding: 30px;
    }
  </style>
</head>
<body>

<div id="ascii-container">Loading animation...</div>

<script>
  const frameCount = 5; // 👈 Change this to match your total number of frames
  let currentFrame = 0;
  let frames = [];
  const container = document.getElementById('ascii-container');

  async function loadFrames() {
    for (let i = 1; i <= frameCount; i++) {
      try {
        const res = await fetch(`frames/frame${i}.txt`);
        const text = await res.text();
        frames.push(text);
      } catch (err) {
        frames.push(`(Frame ${i} missing)`);
      }
    }
  }

  function playAnimation() {
    setInterval(() => {
      container.textContent = frames[currentFrame];
      currentFrame = (currentFrame + 1) % frames.length;
    }, 200); // ⏱ Change speed here (in milliseconds)
  }

  loadFrames().then(playAnimation);
</script>

</body>
</html>
